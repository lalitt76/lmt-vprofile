1  ssh-keygen
    2  sudo apt update && apt install awscli -y
    3  awscli
    4  aws
    5  sudo -i
    6  aws
    7  aws configure
    8  cat .aws/config
    9  ls .aws/
   10  cat .aws/credentials
   11  curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
   12  curl -LO "https://dl.k8s.io/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl.sha256"
   13  echo "$(cat kubectl.sha256)  kubectl" | sha256sum --check
   14  ls
   15  sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl
   16  kubectl --help
   17  kubectl version --client
   18  kubectl version --client --output=yaml
   19  curl -LO https://github.com/kubernetes/kops/releases/download/$(curl -s https://api.github.com/repos/kubernetes/kops/releases/latest | grep tag_name | cut -d '"' -f 4)/kops-linux-amd64
   20  ls
   21  sudo chmod +x kops-linux-amd64
   22  ls
   23  sudo mv kops-linux-amd64 /usr/local/bin/kops
   24  kops
   25  kops -h
   26  nslookup -type=ns kops.tyagienterprises.com
   28  kops create cluster  --name=kops.tyagienterprises.com --state=s3://vprofile-kops-state-lmt --zones=us-east-1a,us-east-1b --node-count=2 --node-size=t3.small --master-size=t3.medium --dns-zone=kops.tyagienterprises.com --node-volume-size=8 --master-volume-size=8
   29  kops get cluster
   30  kops get cluster --state=s3://vprofile-kops-state-lmt
   31  kops update cluster --name kops.tyagienterprises.com --state=s3://vprofile-kops-state-lmt --yes --admin
   32  history
   33  kops validate cluster --state=s3://vprofile-kops-state-lmt
   34  history
   35  kops delete cluster --name kops.tyagienterprises.com --state=s3://vprofile-kops-state-lmt --yes
